# Regras normativas

As regras permanecem versionadas no catálogo local migrado da ferramenta combinada. Cada resultado registra os metadados normativos usados. Competências sem regra cadastrada devem falhar explicitamente; não há fallback silencioso.
